# Quick Start Guide

## 🚀 Start Backend

```bash
cd voiceboat
python3 -m uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
```

**Note:** Use `python3 -m uvicorn` instead of just `uvicorn` if it's not in PATH.

## 🎨 Start Frontend

```bash
cd voiceboat_ui
npm run dev
```

## 🔑 Enable OpenAI (Optional but Recommended)

For natural, human-like conversation:

```bash
export OPENAI_API_KEY="sk-your-api-key-here"
```

Get API key from: https://platform.openai.com/api-keys

**Without OpenAI:** Uses mock adapters (works but less intelligent)
**With OpenAI:** Uses Whisper (ASR) + GPT (NLU) for natural conversation

## 🎯 Test Voice Call

1. Go to `http://localhost:5173/driver`
2. Click the **green phone button** 🟢
3. Bot greets: "हैलो! क्या चाहिए?"
4. Speak naturally: "hello kya jarurat hai?"
5. Bot responds casually like a friend!

## 💡 Features

- ✅ **Casual conversation** - Talks like a friend, not formal
- ✅ **Hindi/English support** - Understands Hinglish perfectly
- ✅ **Proactive interaction** - Bot speaks when you don't
- ✅ **Real-time voice** - Like a phone call
- ✅ **Natural responses** - Generated by GPT (if API key set)

---

**That's it! Your voicebot is ready! 🎉**
